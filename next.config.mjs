/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/cgpa_gpa_calci', // 🔁 Your GitHub repo name here
  assetPrefix: '/cgpa_gpa_calci',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
